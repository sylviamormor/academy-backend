const request = require('supertest');
const { expect } = require('chai');
const app = require('../src/controllers/applicant.controllers');
// const { userData } = require( '../fixtures/user.fixture.js');

// eslint-disable-next-line no-undef
describe('Testing Applicant Controllers', () => {
  // eslint-disable-next-line no-undef
  it('should test create applicant controllers', async () => {

  });

  // eslint-disable-next-line no-undef
  it('should fail login input', async () => {

  });
});
